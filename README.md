# dodomain - Площадка для покупки и продажи доменов

Проект на Next.js 15 с SSR, PostgreSQL и Drizzle ORM. Площадка для покупки и продажи премиум доменов.

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Установка и запуск](#установка-и-запуск)
- [Деплой на сервер](#деплой-на-сервер)
- [База данных](#база-данных)
- [API Routes](#api-routes)
- [Страницы проекта](#страницы-проекта)
- [Компоненты](#компоненты)
- [Разработка](#разработка)

## 🚀 Технологический стек

- **Framework**: Next.js 15 (App Router)
- **React**: 19
- **TypeScript**: 5.x
- **Database**: PostgreSQL
- **ORM**: Drizzle ORM
- **Styling**: Tailwind CSS 4
- **Forms**: react-hook-form + zod
- **Icons**: lucide-react
- **Date handling**: date-fns

## 📁 Структура проекта

```
dodomain/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── actions/           # Server Actions
│   │   │   └── contact.ts     # Форма контактов
│   │   ├── api/               # API Routes
│   │   │   ├── domains/       # API для доменов
│   │   │   ├── blog/          # API для блога
│   │   │   └── health/        # Health check endpoint
│   │   ├── domains/           # Страницы доменов
│   │   │   ├── page.tsx       # Каталог доменов
│   │   │   └── [slug]/        # Детальная страница домена
│   │   ├── blog/              # Страницы блога
│   │   │   ├── page.tsx       # Каталог статей
│   │   │   └── [slug]/        # Страница статьи
│   │   ├── globals.css        # Глобальные стили
│   │   ├── layout.tsx         # Корневой layout
│   │   ├── page.tsx           # Главная страница
│   │   ├── sitemap.ts         # Sitemap
│   │   └── robots.ts          # Robots.txt
│   ├── components/            # React компоненты
│   │   ├── domains/          # Компоненты доменов
│   │   ├── blog/             # Компоненты блога
│   │   ├── forms/            # Формы
│   │   ├── layout/           # Layout компоненты
│   │   └── ui/               # UI компоненты
│   ├── db/                   # База данных
│   │   ├── index.ts          # Drizzle клиент
│   │   ├── schema.ts         # Схема базы данных
│   │   └── seed.ts           # Seed скрипт
│   └── lib/                  # Утилиты
│       ├── utils.ts          # Вспомогательные функции
│       └── constants.ts      # Константы
├── public/                   # Статические файлы
├── scripts/                 # Скрипты деплоя
│   ├── deploy.sh            # Скрипт деплоя
│   └── backup.sh           # Скрипт бэкапа БД
├── nginx/                   # Конфигурация Nginx
│   └── nginx.conf          # Reverse proxy конфигурация
├── drizzle.config.ts         # Конфигурация Drizzle
├── next.config.ts            # Конфигурация Next.js
├── docker-compose.yml        # Docker Compose конфигурация
├── Dockerfile              # Docker multi-stage build
├── package.json              # Зависимости
├── tsconfig.json             # Конфигурация TypeScript
├── .env.local                # Переменные окружения
├── .dockerignore            # Исключения для Docker
├── DEPLOY.md               # Документация деплоя
└── README.md               # Этот файл
```

## 🔧 Установка и запуск

### Требования

- Node.js 20+
- PostgreSQL 14+

### Установка зависимостей

```bash
npm install
```

### Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
# База данных PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/dodomain"

# Базовый URL сайта
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
```

### Настройка базы данных

1. Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE dodomain;
```

2. Выполните миграции:

```bash
npm run db:push
```

3. Заполните базу тестовыми данными:

```bash
npm run db:seed
```

### Запуск проекта

Режим разработки:

```bash
npm run dev
```

Сборка для продакшена:

```bash
npm run build
npm start
```

## 🚀 Деплой на сервер

Для деплоя на собственный сервер используйте Docker и Docker Compose.

### Быстрый старт (одна команда)

```bash
# Клонирование и быстрый деплой одной командой
git clone https://github.com/your-username/dodomain.git /var/www/dodomain && \
cd /var/www/dodomain && \
sudo ./scripts/quick-deploy.sh
```

### Ручной деплой

1. Установите Docker и Docker Compose на сервер
2. Клонируйте репозиторий: `git clone https://github.com/your-username/dodomain.git`
3. Настройте `.env` файл
4. Запустите деплой: `sudo ./scripts/deploy.sh`

### Полная инструкция

Подробное руководство по деплою смотрите в файле [`DEPLOY.md`](DEPLOY.md), включая:

- Установку Docker и Docker Compose
- Настройку SSL сертификатов (Let's Encrypt)
- Настройку Nginx reverse proxy
- Автоматическое резервное копирование
- Мониторинг и логи
- Troubleshooting

### Docker команды

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка контейнеров
docker-compose down

# Перезапуск
docker-compose restart
```

### Скрипты деплоя

- [`scripts/deploy.sh`](scripts/deploy.sh) - автоматический деплой проекта
- [`scripts/backup.sh`](scripts/backup.sh) - бэкап и восстановление базы данных

## 🗄️ База данных

### Схема базы данных

#### Таблица `domains`

| Поле             | Тип          | Описание         |
| ---------------- | ------------ | ---------------- |
| id               | serial       | Первичный ключ   |
| name             | varchar(255) | Имя домена       |
| price            | integer      | Цена             |
| category         | varchar(100) | Категория        |
| extension        | varchar(20)  | Доменная зона    |
| description      | text         | Описание         |
| length           | integer      | Длина домена     |
| featured         | boolean      | Рекомендуемый    |
| trafficLevel     | varchar(50)  | Уровень трафика  |
| seoPotential     | integer      | SEO потенциал    |
| registeredYear   | integer      | Год регистрации  |
| registrationDate | timestamp    | Дата регистрации |
| listedDate       | timestamp    | Дата добавления  |
| isActive         | boolean      | Активен          |
| createdAt        | timestamp    | Дата создания    |
| updatedAt        | timestamp    | Дата обновления  |

#### Таблица `blog_posts`

| Поле            | Тип          | Описание         |
| --------------- | ------------ | ---------------- |
| id              | serial       | Первичный ключ   |
| slug            | varchar(255) | URL slug         |
| title           | varchar(255) | Заголовок        |
| excerpt         | text         | Краткое описание |
| content         | text         | Содержание       |
| category        | varchar(100) | Категория        |
| readTime        | varchar(50)  | Время чтения     |
| featuredImage   | varchar(500) | Изображение      |
| publishedDate   | timestamp    | Дата публикации  |
| isPublished     | boolean      | Опубликован      |
| metaTitle       | varchar(255) | Meta заголовок   |
| metaDescription | text         | Meta описание    |
| createdAt       | timestamp    | Дата создания    |
| updatedAt       | timestamp    | Дата обновления  |

#### Таблица `contact_submissions`

| Поле       | Тип          | Описание       |
| ---------- | ------------ | -------------- |
| id         | serial       | Первичный ключ |
| name       | varchar(255) | Имя            |
| email      | varchar(255) | Email          |
| subject    | varchar(500) | Тема           |
| message    | text         | Сообщение      |
| domainId   | integer      | ID домена      |
| domainName | varchar(255) | Имя домена     |
| type       | varchar(50)  | Тип заявки     |
| status     | varchar(50)  | Статус         |
| createdAt  | timestamp    | Дата создания  |

#### Таблица `domain_offers`

| Поле       | Тип          | Описание           |
| ---------- | ------------ | ------------------ |
| id         | serial       | Первичный ключ     |
| domainName | varchar(255) | Имя домена         |
| offerPrice | integer      | Предложенная цена  |
| buyerName  | varchar(255) | Имя покупателя     |
| buyerEmail | varchar(255) | Email покупателя   |
| buyerPhone | varchar(50)  | Телефон покупателя |
| message    | text         | Сообщение          |
| status     | varchar(50)  | Статус             |
| createdAt  | timestamp    | Дата создания      |
| updatedAt  | timestamp    | Дата обновления    |

### Команды Drizzle

```bash
# Генерация миграций
npm run db:generate

# Применение миграций
npm run db:migrate

# Push схемы (без миграций)
npm run db:push

# Drizzle Studio (UI для базы данных)
npm run db:studio

# Заполнение тестовыми данными
npm run db:seed
```

## 📡 API Routes

### GET /api/domains

Получение списка доменов с фильтрацией и пагинацией.

**Query параметры:**

- `page` - номер страницы (по умолчанию: 1)
- `limit` - количество на странице (по умолчанию: 20)
- `search` - поиск по имени
- `category` - фильтр по категории
- `extension` - фильтр по доменной зоне
- `minLength` - минимальная длина
- `maxLength` - максимальная длина
- `sortBy` - сортировка (featured, price_asc, price_desc, length_asc, length_desc, newest)

### GET /api/domains/[slug]

Получение домена по имени.

### GET /api/blog

Получение списка статей блога с фильтрацией и пагинацией.

**Query параметры:**

- `page` - номер страницы (по умолчанию: 1)
- `limit` - количество на странице (по умолчанию: 10)
- `search` - поиск по заголовку и содержимому
- `category` - фильтр по категории
- `sortBy` - сортировка (newest, oldest)

### GET /api/blog/[slug]

Получение статьи по slug.

## 📄 Страницы проекта

| Путь              | Описание                  | Тип компонента |
| ----------------- | ------------------------- | -------------- |
| `/`               | Главная страница          | Server         |
| `/domains`        | Каталог доменов           | Server         |
| `/domains/[slug]` | Детальная страница домена | Server         |
| `/blog`           | Каталог статей            | Server         |
| `/blog/[slug]`    | Страница статьи           | Server         |
| `/contact`        | Контакты                  | Server         |
| `/about`          | О нас                     | Server         |
| `/sell-domain`    | Продать домен             | Server         |

## 🧩 Компоненты

### Layout компоненты

- `Header` - Навигационный хедер (Client)
- `Footer` - Футер (Server)
- `Logo` - Логотип (Server)

### UI компоненты

- `CustomSelect` - Кастомный селект с поиском (Client)
- `ContactModal` - Модальное окно контактов (Client)
- `Breadcrumbs` - Хлебные крошки (Server)
- `SEO` - SEO мета-теги (Server)
- `ScrollToTop` - Кнопка "Наверх" (Client)

### Домены

- `DomainCard` - Карточка домена
- `DomainFilters` - Фильтры доменов
- `DomainGrid` - Сетка доменов с пагинацией

### Блог

- `BlogCard` - Карточка статьи
- `BlogGrid` - Сетка статей с пагинацией

### Формы

- `ContactForm` - Форма контактов с валидацией

## 🔨 Разработка

### Server Actions

Проект использует Server Actions для обработки форм:

```typescript
import { submitContactForm } from "@/app/actions/contact";

// Использование в компоненте
await submitContactForm(formData);
```

### Server Components vs Client Components

**Server Components** (по умолчанию):

- Рендерятся на сервере
- Не могут использовать useState, useEffect
- Имеют доступ к базе данных напрямую
- Лучше для SEO и производительности

**Client Components** (с директивой `'use client'`):

- Рендерятся на клиенте
- Могут использовать хуки React
- Требуют для интерактивности (формы, модальные окна)

### Категории доменов

- Брендовые
- Короткие
- Ключевые слова
- Географические
- Имена
- Технологии
- Финансы
- Бизнес
- Здоровье
- Спорт
- Образование
- Развлечения
- Новости
- Интернет
- Торговля

### Доменные зоны

- `.ru`, `.рф`, `.рус`, `.ru.com`
- `.com`, `.net`, `.org`, `.info`
- `.su`, `.moscow`, `.москва`
- `.дети`, `.онлайн`, `.сайт`
- `.name`, `.ru.net`, `.com.ru`
- `.xyz`, `.site`, `.online`
- `.io`, `.ai`, `.co`, `.me`

## 📝 Лицензия

MIT

## 👥 Контакты

- Telegram: [@pnlup](https://t.me/pnlup)
- Email: info@dodomain.ru
