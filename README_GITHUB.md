# Dodomain - Next.js Migration Project

🚀 **Полная миграция проекта Dodomain с React на Next.js 15**

## 📋 О проекте

Современная платформа для продажи доменов, построенная на Next.js 15 с использованием App Router, Server-Side Rendering и PostgreSQL.

## ✨ Особенности

- ⚡ **Next.js 15** с App Router
- 🎨 **Server-Side Rendering (SSR)** для всех страниц
- 🗄️ **PostgreSQL** + Prisma ORM
- 📱 **Адаптивный дизайн**
- 🔍 **SEO-оптимизация**
- 🚀 **Высокая производительность**

## 📦 Что включено

### Страницы (9 шт):
1. Главная (`/`)
2. Каталог доменов (`/domains`)
3. Страница домена (`/domains/[name]`)
4. О нас (`/about`)
5. Контакты (`/contact`)
6. Блог (`/blog`)
7. Статья блога (`/blog/[slug]`)
8. Продать домен (`/sell-domain`)
9. 404 страница

### API Endpoints (5 шт):
- `/api/domains` - Список доменов
- `/api/domains/[name]` - Информация о домене
- `/api/blog` - Список статей
- `/api/blog/[slug]` - Статья блога
- `/api/contact` - Обработка формы контактов

## 🛠️ Технологии

- **Frontend**: Next.js 15, React 19, TypeScript
- **Backend**: Next.js API Routes
- **Database**: PostgreSQL + Prisma ORM
- **Styling**: CSS Modules
- **Deployment**: PM2 + Nginx

## 🚀 Быстрый старт

### Автоматическая установка (Рекомендуется)

```bash
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматически установит и настроит:
- Node.js 18.x
- PostgreSQL
- Nginx
- PM2
- SSL (опционально)

### Ручная установка

1. **Установите зависимости:**
```bash
npm install
```

2. **Настройте .env.local:**
```env
DATABASE_URL="postgresql://user:password@localhost:5432/dodomain"
NEXT_PUBLIC_SITE_URL=https://dodomain.ru
```

3. **Инициализируйте базу данных:**
```bash
npx prisma generate
npx prisma db push
```

4. **Запустите dev-сервер:**
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 📚 Документация

- [Инструкция по развертыванию](./DEPLOYMENT_INSTRUCTIONS.md)
- [Быстрый старт для клиента](./QUICK_START_FOR_CLIENT.md)
- [MVP заметки](./MVP_NOTE.md)

## 🗄️ База данных

Проект использует PostgreSQL с Prisma ORM.

### Модели:
- **Domain** - Домены для продажи
- **BlogPost** - Статьи блога
- **ContactMessage** - Сообщения из формы контактов

### Команды Prisma:
```bash
npx prisma studio        # Открыть админ-панель БД
npx prisma generate      # Генерация клиента
npx prisma db push       # Применить схему к БД
npx prisma db seed       # Заполнить тестовыми данными
```

## 🔧 Управление

### Development
```bash
npm run dev          # Запуск dev-сервера
npm run build        # Сборка для production
npm run start        # Запуск production-сервера
npm run lint         # Проверка кода
```

### Production (PM2)
```bash
pm2 start npm --name "dodomain" -- start
pm2 status           # Статус
pm2 logs dodomain    # Логи
pm2 restart dodomain # Перезапуск
pm2 stop dodomain    # Остановка
```

## 📊 Структура проекта

```
dodomain-next/
├── app/                    # Next.js App Router
│   ├── page.tsx           # Главная страница
│   ├── domains/           # Каталог доменов
│   ├── blog/              # Блог
│   ├── contact/           # Контакты
│   ├── about/             # О нас
│   ├── sell-domain/       # Продать домен
│   ├── api/               # API Routes
│   └── layout.tsx         # Общий layout
├── components/            # React компоненты
│   ├── Header.tsx
│   ├── Footer.tsx
│   └── Logo.tsx
├── lib/                   # Утилиты
│   └── prisma.ts         # Prisma клиент
├── prisma/               # Prisma схема и миграции
│   ├── schema.prisma
│   └── seed.ts
├── public/               # Статические файлы
├── types/                # TypeScript типы
├── deploy.sh             # Скрипт деплоя
└── package.json

```

## 🌐 Деплой

Проект готов к развертыванию на любом VPS с Ubuntu/Debian.

**Минимальные требования:**
- Ubuntu 20.04+ / Debian 11+
- 1GB RAM
- 10GB диск
- Node.js 18+
- PostgreSQL 14+

## 📝 Лицензия

Проект разработан для клиента Binance (Георгий Тагулия) на платформе Kwork.

## 👨‍💻 Разработчик

Разработано с ❤️ для Dodomain

---

**Статус:** ✅ Готов к production
**Версия:** 1.0.0
**Дата:** Декабрь 2024
