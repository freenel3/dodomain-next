# 🚀 Инструкция по развертыванию Dodomain на сервере

## 📋 Что будет установлено

Автоматический скрипт установит и настроит:
- ✅ Node.js 18.x
- ✅ PostgreSQL (база данных)
- ✅ Nginx (веб-сервер)
- ✅ PM2 (менеджер процессов)
- ✅ SSL сертификат (опционально)

---

## 🔧 Шаги для развертывания

### 1️⃣ Подключитесь к серверу по SSH

```bash
ssh root@94.232.41.46
```

Пароль: `Bq93%nRoK`

---

### 2️⃣ Установите Git (если не установлен)

```bash
apt-get update
apt-get install -y git
```

---

### 3️⃣ Клонируйте проект

Я отправлю вам ссылку на приватный GitHub репозиторий с проектом.

```bash
cd /var/www
git clone <ССЫЛКА_НА_РЕПОЗИТОРИЙ> dodomain
cd dodomain
```

---

### 4️⃣ Запустите автоматический деплой

```bash
chmod +x deploy.sh
./deploy.sh
```

**Скрипт автоматически:**
- Установит все необходимое ПО
- Создаст базу данных
- Настроит Nginx
- Запустит приложение

**Время выполнения:** 5-10 минут

---

### 5️⃣ Проверьте работу сайта

Откройте в браузере: **http://dodomain.ru**

Вы должны увидеть главную страницу сайта!

---

## 📊 Управление приложением

### Просмотр статуса
```bash
pm2 status
```

### Просмотр логов
```bash
pm2 logs dodomain
```

### Перезапуск приложения
```bash
pm2 restart dodomain
```

### Остановка приложения
```bash
pm2 stop dodomain
```

---

## 🔒 SSL сертификат (HTTPS)

Если хотите включить HTTPS, во время установки ответьте `y` на вопрос о SSL.

Или установите позже:
```bash
apt-get install -y certbot python3-certbot-nginx
certbot --nginx -d dodomain.ru -d www.dodomain.ru
```

---

## 🗄️ База данных

**Параметры подключения:**
- Host: `localhost`
- Port: `5432`
- Database: `dodomain`
- User: `dodomain_user`
- Password: `dodomain_password_2024`

---

## 📝 Структура проекта

```
/var/www/dodomain/
├── app/              # Страницы Next.js
├── components/       # Компоненты React
├── prisma/          # Схема базы данных
├── public/          # Статические файлы
├── .env.local       # Переменные окружения
└── deploy.sh        # Скрипт деплоя
```

---

## 🆘 Решение проблем

### Приложение не запускается
```bash
pm2 logs dodomain --lines 50
```

### Nginx не работает
```bash
sudo nginx -t
sudo systemctl status nginx
```

### База данных не подключается
```bash
sudo systemctl status postgresql
sudo -u postgres psql -c "\l"
```

---

## 📞 Поддержка

Если возникнут проблемы при установке:
1. Сделайте скриншот ошибки
2. Отправьте мне в Kwork
3. Я помогу решить проблему

---

## ✅ Чеклист после установки

- [ ] Сайт открывается по адресу dodomain.ru
- [ ] Все страницы работают (главная, домены, блог, контакты)
- [ ] База данных содержит тестовые данные
- [ ] PM2 показывает статус "online"
- [ ] Nginx работает без ошибок

---

## 🎉 Готово!

После успешной установки сайт будет работать 24/7 автоматически.

PM2 автоматически перезапустит приложение при сбое и после перезагрузки сервера.
